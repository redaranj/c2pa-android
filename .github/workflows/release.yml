name: Release

on:
  push:
    branches: [ chore/reorganize-repo ]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version for the release (e.g. 1.0.0)'
        required: true
        type: string

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Set version
      run: |
        if [[ "${{ github.event_name }}" == "push" ]]; then
          echo "VERSION=0.0.1" >> $GITHUB_ENV
        else
          echo "VERSION=${{ github.event.inputs.version }}" >> $GITHUB_ENV
        fi
    
    - name: Validate version format
      run: |
        # Simple validation for version format
        if [[ ! "${{ env.VERSION }}" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
          echo "::error::Version must be in format X.Y.Z (e.g., 1.0.0)"
          exit 1
        fi
    
    - name: Configure Git user
      run: |
        git config user.name "GitHub Actions"
        git config user.email "actions@github.com"

    - name: Create tag
      run: |
        git tag ${{ env.VERSION }}
        git push origin ${{ env.VERSION }}

    - name: Display release information
      run: |
        echo "Starting release process for version ${{ env.VERSION }}"
        echo "This workflow will:"
        echo "1. Download pre-built Android libraries"
        echo "2. Build the Android AAR"
        echo "3. Create a GitHub release with artifacts"

    - name: Set up JDK 17
      id: setup-java
      uses: actions/setup-java@v3
      with:
        java-version: "17"
        distribution: "temurin"

    - name: Set up Android SDK
      uses: android-actions/setup-android@v3

    - name: Set environment variables
      run: |
        echo "ANDROID_HOME=$ANDROID_SDK_ROOT" >> $GITHUB_ENV

    - name: Build Android AAR
      env:
        JAVA_HOME: ${{ steps.setup-java.outputs.path }}
      run: |
        make library

    - name: List AAR artifacts
      run: |
        echo "Built AAR files:"
        find library/build/outputs/aar -name "*.aar" -type f

    - name: Publish to GitHub Packages
      env:
        JAVA_HOME: ${{ steps.setup-java.outputs.path }}
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        CI_COMMIT_TAG: ${{ env.VERSION }}
      run: |
        make publish

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ env.VERSION }}
        name: C2PA Android ${{ env.VERSION }}
        files: |
          library/build/outputs/aar/*.aar
        body: |
          # C2PA Android Release ${{ env.VERSION }}

          ## Android Integration

          ### Gradle dependency

          Add the AAR library to your Android project:

          1. Download the AAR file from this release
          2. Place it in your app's `libs` directory
          3. Add to your `build.gradle`:

          ```gradle
          dependencies {
              implementation files('libs/c2pa-release.aar')
          }
          ```

          ### Usage

          See the [README.md](https://github.com/${{ github.repository }}) for detailed integration instructions and API documentation.

          ## What's Changed

          See the [commit history](https://github.com/${{ github.repository }}/compare/{{ previousTag }}...${{ env.VERSION }}) for a full list of changes.
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
