<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <!-- Suppress TrustAllX509TrustManager warning from BouncyCastle library (external dependency) -->
    <issue id="TrustAllX509TrustManager">
        <ignore regexp="bcpkix-jdk18on.*\.jar" />
    </issue>
    
    <!-- Document that 16KB alignment is a known issue for native libraries -->
    <!-- This requires rebuilding the C2PA native libraries with proper alignment -->
    <issue id="Aligned16KB" severity="informational" />
</lint>